const { error } = require('console');
const fs = require ('fs');
const archivo_datos = "datos.txt";
fs.readfile (archivo_datos, 'UTF8', (error datos) => {
    if (error){
        console.err ("error in err"; error);
    }          + ("new.txt")
    const nuevo contenido = datos + new date () tolocalestring();
}